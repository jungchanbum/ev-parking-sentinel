# 기초부터 이해하기 — object_detect 입문 가이드

> [README.md](README.md) 가 "**무엇을 만들었나**" 라면, 이 문서는 "**이게 다 무슨 소리냐**" 를 다룬다.
> 사전지식 없이 읽을 수 있게 썼다. 용어가 나오면 바로 그 자리에서 설명한다.

---

## 0. 한 문장 요약

> **카메라 안에서 돌아가는 작은 프로그램을 만들어서, 카메라의 AI가 찾아낸 사람들 중
> "실제로 움직이는 사람"만 골라내고, 그걸 웹페이지에서 영상 위에 네모로 보여준다.**

---

## 1. 이게 왜 특이한 프로젝트인가

보통 프로그램은 **내 컴퓨터**에서 돈다. 근데 이 앱은 **카메라 안**에서 돈다.

```
일반적인 CCTV 구성                    우리가 만든 것
─────────────────                    ──────────────
카메라 → (영상 전송) → 서버           카메라 ┌─────────────┐
                       └ 서버가        │  │ 우리 앱이     │
                         분석          │  │ 여기서 분석!  │
                                       │  └─────────────┘
```

이렇게 **기기 안에서 직접 분석**하는 걸 **엣지 컴퓨팅(Edge Computing)** 이라고 한다.
장점: 영상을 밖으로 안 보내도 되고(사생활), 네트워크도 안 먹고, 지연이 없다.

한화비전 카메라는 이런 앱을 만들어 넣을 수 있게 **OpenSDK(오픈 플랫폼)** 를 제공한다.
스마트폰에 앱 설치하듯, **카메라에 앱을 설치**하는 것이다.

---

## 2. 등장인물 소개

| 이름 | 정체 | 역할 |
|------|------|------|
| **카메라** | PNM-C16083RVQ | 하드웨어. 렌즈 4개짜리 다방향 카메라 |
| **WiseAI** | 카메라에 내장된 AI | 영상 보고 "저기 사람 있다" 찾아냄 (우리가 만든 게 아님) |
| **object_detect** | **우리가 만든 앱** | WiseAI 결과를 받아 "움직이는지" 판단 |
| **OpenSDK** | 한화가 준 개발도구 | 카메라 앱을 만들 수 있게 해주는 틀 |
| **Docker** | 개발 환경 | 카메라용 프로그램을 만들어내는 공장 |
| **브라우저** | 크롬/엣지 | 결과를 보는 화면 |

**중요**: WiseAI가 "사람 찾기"는 이미 다 해준다.
우리가 만든 건 **"그 사람이 움직이는지 판단하는 부분"** 이다.

---

## 3. 용어 사전 (핵심만, 나오는 순서대로)

### 3.1 카메라/영상 쪽 용어

| 용어 | 쉬운 설명 | 우리 프로젝트에선 |
|------|-----------|-------------------|
| **프레임(Frame)** | 영상의 사진 1장. 영상 = 사진을 빠르게 넘긴 것 | WiseAI가 프레임마다 분석 결과를 줌 (초당 약 5장) |
| **메타데이터(Metadata)** | 영상 자체가 아니라 **영상에 대한 정보**. "이 프레임 어디에 사람이 있다" 같은 것 | 우리가 받는 게 바로 이것 |
| **바운딩 박스(Bounding Box)** | 물체를 감싸는 네모 상자. AI가 "여기 있다"고 표시하는 방법 | `left/top/right/bottom` 4개 숫자 |
| **채널(Channel)** | 카메라의 렌즈/영상 하나. 이 카메라는 다방향이라 채널이 여러 개 | `ch0`, `ch1` … 각각 따로 감지 |
| **ONVIF** | CCTV 업계 **표준 규격**. 제조사가 달라도 통하게 만든 약속 | 메타데이터가 ONVIF 형식(XML)으로 옴 |
| **SUNAPI** | 한화 카메라 **자체 API** 규격 | 스냅샷 URL 등이 이 규격 |

### 3.2 프로그래밍/구조 용어

| 용어 | 쉬운 설명 | 우리 프로젝트에선 |
|------|-----------|-------------------|
| **컴포넌트(Component)** | 기능 담당 부서/직원 하나 | `SampleComponent` (우리 코드 전부가 여기 들어있음) |
| **이벤트(Event)** | 부서끼리 주고받는 쪽지 | "메타데이터 왔다", "HTTP 요청 왔다" |
| **스케줄러(Scheduler)** | 그 직원의 책상 + **받은편지함(큐)** | `SampleComponentScheduler` |
| **큐(Queue)** | 순서대로 쌓이는 대기줄 | 이게 없으면 쪽지를 못 받음 (8장 사건 ①) |
| **컨테이너(Container)** | 우리 앱이라는 **건물** 하나 | `object_detect` |
| **매니페스트(Manifest)** | **조직도/배선도**. 누가 누구 소식을 듣고 누구에게 보내는지 적은 JSON 파일 | 코드보다 이게 더 중요했음! |
| **Receiver(수신자)** | 내가 **보낼** 대상 | `AppDispatcher`, `OpenPlatform` |
| **Source(소스)** | 내가 **구독할** 소식통 | `MetadataManager_{Ch}` |

### 3.3 웹 쪽 용어

| 용어 | 쉬운 설명 | 우리 프로젝트에선 |
|------|-----------|-------------------|
| **HTTP** | 웹에서 요청/응답 주고받는 규칙 | 브라우저가 앱에게 데이터 달라고 할 때 |
| **엔드포인트(Endpoint)** | 앱이 열어둔 **창구 주소** | `/detections`, `/snapshot` |
| **JSON** | 데이터 주고받는 **표준 양식** | `{"id":2018,"moving":true}` |
| **fetch** | JS가 서버에 데이터 요청하는 명령 | 브라우저가 감지결과 가져올 때 |
| **폴링(Polling)** | 계속 반복해서 물어보기 ("왔어? 왔어?") | 150ms마다 감지결과 요청 |
| **캔버스(Canvas)** | HTML에서 **그림 그리는 판** | 영상 위에 네모 그릴 때 |

### 3.4 빌드/배포 용어

| 용어 | 쉬운 설명 | 우리 프로젝트에선 |
|------|-----------|-------------------|
| **컴파일(Compile)** | 사람이 쓴 코드 → 기계가 읽는 파일로 번역 | C++ → `.so` |
| **크로스 컴파일** | **내 PC(x86)에서 카메라(ARM)용** 프로그램 만들기 | 그래서 도커가 필요 |
| **도커(Docker)** | 개발환경이 통째로 든 **상자** | 한화가 준 `opensdk` 이미지 |
| **패키징** | 설치 가능한 파일로 포장 | `.cap` 파일 생성 |
| **`.cap`** | 카메라용 **앱 설치 파일** (안드로이드 `.apk` 같은 것) | 이걸 카메라 웹에 업로드 |

---

## 4. OpenSDK 앱은 도대체 어떻게 생겼나

### 회사에 비유하면

```
object_detect (회사 = 컨테이너)
  │
  ├─ SampleComponent (직원 = 컴포넌트)     ← 우리 코드가 여기 다 들어감
  │     책상 = SampleComponentScheduler
  │     받은편지함(큐) ← 여기로 쪽지가 옴
  │
  └─ AppDispatcher (직원)                  ← 한화가 만들어준 것. 웹 요청 배달부
```

**카메라 시스템(System)** 이라는 **다른 회사**가 옆에 있다:
```
System (카메라 본체)
  ├─ MetadataManager_0~3   ← WiseAI 결과를 뿌리는 방송국
  ├─ OpenAPIDispatcher     ← 웹 요청 받는 곳
  └─ OpenPlatform          ← 스냅샷 등 기능 제공
```

우리 앱은 **다른 회사(System)의 방송을 구독**해야 한다. 그걸 적는 게 **매니페스트**다.

### 매니페스트가 하는 일 (제일 헷갈리는 부분)

#### 유튜브로 비유하면 딱 맞다

| 유튜브 | 우리 앱 | 뜻 |
|--------|---------|-----|
| **구독한 채널** | **Source(소스)** | 여기서 **알림(이벤트)이 나한테 온다** |
| 알림 오면 내가 하는 일 | **C++ 코드** | 영상 보기 / 이벤트 처리하기 |
| **친구 목록** | **Receiver(수신자)** | 내가 **메시지를 보낼** 상대 |
| 구독/친구 목록이 저장된 곳 | **매니페스트(JSON)** | 설정 파일 |

즉:
- **Source = 내가 구독하는 쪽** (나에게 이벤트가 **들어옴**) ← `MetadataManager`
- **Receiver = 내가 보내는 쪽** (내가 이벤트를 **내보냄**) ← `AppDispatcher`

#### 왜 코드가 아니라 JSON에 적나?

**구독 목록은 "일하는 방법"이 아니라 "설정"이기 때문.**

```
C++ 코드      = 알림이 오면 무엇을 할지        (일하는 방법)
매니페스트     = 누구를 구독하고 누구에게 보낼지 (설정)
```

유튜브 앱을 새로 설치한다고 구독 목록이 코드에 박혀있진 않다. 설정에 있다.
마찬가지로, 카메라는 **앱 시작할 때 이 JSON을 읽고** 배선을 연결해준다.
→ 코드를 고치지 않아도 **JSON만 바꾸면 구독 대상이 바뀐다.**

> 💡 **이 프로젝트에서 제일 크게 데인 부분이 바로 이것.**
> 코드는 멀쩡한데 **JSON 배선이 틀려서** 이벤트가 아예 안 들어왔다.
> "코드보다 매니페스트가 더 중요했다"는 게 이 뜻.

#### 실제 파일 보기

C++ 코드에는 "누구한테 받을지" 안 적는다. **JSON 파일에 적는다.**

```json
// SampleComponent_manifest_instance_0.json
{
  "InstanceName": "SampleComponent",        // 내 이름
  "SchedulerName": "SampleComponentScheduler",  // 내 책상
  "ReceiverNames": ["AppDispatcher", "OpenPlatform"],   // 내가 보낼 대상들
  "SourceNames": [
    { "Source": "MetadataManager_{Ch}", "GroupName": "Metadata::OpenApp" }
    //  ↑ "메타데이터 방송국"의 "OpenApp 채널"을 구독하겠다
  ],
  "Channel": "System"
}
```

그리고 **"MetadataManager_{Ch}가 어디 있는 누구냐"** 는 또 다른 파일에 적는다:

```json
// PLifeCycleManagermanifest.json
"RemoteContainerNames": [
  { "ContainerName": "System", "Address": "localhost", "PortNumber": 8587 }
  //  ↑ 카메라 본체는 localhost:8587 에 있다
],
"RemoteComponentNames": [
  { "LocalComponentName": "MetadataManager_{Ch}",
    "ContainerName": "System",
    "RemoteComponentName": "MetadataManager_{Ch}" }
  //  ↑ 내가 "MetadataManager_0" 이라 부르는 건, System 회사의 "MetadataManager_0" 이다
]
```

> **`{Ch}` 가 뭔가?** 채널 번호가 들어갈 **빈칸(템플릿)**.
> 프레임워크가 알아서 `MetadataManager_0`, `_1`, `_2`, `_3` 로 늘려준다.
> **이걸 안 쓰고 직접 0,1,2,3 적었다가 큐가 통째로 안 만들어졌다** (README 8장 사건 ①).

### 코드는 어떻게 생겼나 — 아주 천천히

#### (1) 먼저 "함수"가 뭔지부터

**함수 = 이름을 붙인 작업 묶음.** 요리 레시피라고 보면 된다.

```
레시피 이름: 라면끓이기
필요한 재료: 물, 면
하는 일:     물 끓이기 → 면 넣기 → 3분 기다리기
결과물:      라면 한 그릇
```

프로그램은 이런 **레시피(함수)들의 모음**이다.
누가 "라면끓이기!" 하고 **부르면(호출하면)** 그 안의 내용이 실행된다.

#### (2) C++ 기호 읽는 법 (이것만 알면 됨)

| 기호 | 뜻 | 예 |
|------|-----|-----|
| `//` | **주석**. 사람이 읽는 메모. 실행 안 됨 | `// 여긴 설명` |
| `{ }` | **묶음**. "여기부터 여기까지가 한 덩어리" | 함수 내용을 감쌈 |
| `;` | **문장 끝**. 한국어의 마침표 | `밥먹기();` |
| `( )` | **함수 부르기** + 재료 넣기 | `라면끓이기(물, 면)` |
| `->` | **"~의"**. 화살표 앞것의 속성/기능 | `event->GetType()` = "이벤트**의** 종류" |
| `::` | **소속**. "누구네 집 ~" | `SampleComponent::` = "SampleComponent네" |
| `*` | 실물이 아니라 **위치를 가리키는 쪽지**(포인터) | `Event* event` = 이벤트가 있는 곳 |

> **포인터(`*`)가 어렵다면**: 지금은 그냥 "**물건 자체가 아니라 물건 있는 자리 번호**"라고만 알면 된다.
> 무거운 짐(이벤트)을 통째로 복사해서 넘기지 말고, "저기 있어" 하고 위치만 알려주는 것.

#### (3) 이제 함수 한 줄을 해부

```cpp
bool SampleComponent::ProcessAEvent(Event* event) {
```

이 한 줄을 조각내면:

| 조각 | 뜻 |
|------|-----|
| `bool` | 이 함수가 **끝나고 돌려줄 답의 종류** = 참/거짓 (`true`/`false`) |
| `SampleComponent::` | 이 함수는 **SampleComponent 소속**이다 |
| `ProcessAEvent` | 함수 **이름**. "Process(처리해라) A(하나의) Event(이벤트를)" |
| `(Event* event)` | **받는 재료**: 이벤트 하나 (`event`라는 이름으로 부르겠다) |
| `{` | **여기서부터 내용 시작** |

한국어로 옮기면:
> **"SampleComponent네 'ProcessAEvent'라는 레시피. 재료로 이벤트 하나를 받고, 끝나면 참/거짓을 돌려준다."**

#### (4) 안쪽 내용 해부

```cpp
  switch (event->GetType()) {
```
- `event->GetType()` = **"이 쪽지의 종류를 알려줘"** 라고 물어보기
- `switch (...)` = **"이 값에 따라 길을 나눠라"** (갈림길)

```cpp
    case eMetadataRequest:
      HandleMetadataEvent(event);
      break;
```
- `case eMetadataRequest:` = **"만약 '메타데이터 왔다' 종류라면"**
- `HandleMetadataEvent(event);` = **그 레시피를 부른다** (쪽지를 넘겨주면서)
- `break;` = **"여기서 끝. 다음 갈림길은 안 봐도 됨"**

#### (5) 전체를 한국어로

```cpp
bool SampleComponent::ProcessAEvent(Event* event) {   // 쪽지 하나 처리하는 레시피
  switch (event->GetType()) {                          // 쪽지 종류를 보고 갈림길
    case eMetadataRequest:                             //  ├ "메타데이터 왔다"면
      HandleMetadataEvent(event);                      //  │   → 메타데이터 처리 레시피 실행
      break;                                           //  │   끝
    case eHttpRequest:                                 //  └ "웹 요청 왔다"면
      HandleHttpRequest(event);                        //      → 웹 응답 레시피 실행
      break;                                           //      끝
  }
}
```

> **"쪽지가 오면, 종류를 보고, 맞는 담당자에게 넘긴다."**
> 회사 안내데스크가 하는 일과 똑같다.

#### (6) 그래서 앱의 전부는?

```
   무한 반복
   ┌──────────────────────────────┐
   │ 1. 받은편지함에서 쪽지 꺼내기 │
   │ 2. ProcessAEvent 실행         │
   │ 3. 종류 보고 담당 함수 호출   │
   └──────────────────────────────┘
              ↑ 카메라가 알아서 돌려줌 (우리가 안 짜도 됨)
```

**우리가 짠 건 3번(담당 함수의 내용)뿐이다.**
1, 2번 반복은 OpenSDK 프레임워크가 알아서 해준다.

**모든 게 이벤트다.** 받은편지함에서 쪽지를 꺼내 종류별로 처리하는 게 앱의 전부.

---

## 5. 좌표와 박스 이해하기

### 픽셀 좌표

화면은 격자다. 왼쪽 위가 `(0,0)`, 오른쪽 아래로 갈수록 숫자가 커진다.

```
(0,0) ─────────────────► x
  │   ┌──────────┐
  │   │          │  ← BoundingBox
  │   │  사람    │     left=1343, top=0
  │   │          │     right=1783, bottom=128
  │   └──────────┘
  ▼
  y
```

우리 카메라 프레임은 **2591 × 1520 픽셀**이었다.

### 프레임 크기를 어떻게 알았나

메타데이터에 이런 게 들어있다:
```xml
<tt:Scale x="0.000772" y="-0.001316"/>
```
ONVIF는 화면을 **-1 ~ +1** 범위로 표현하기로 약속했다. 그래서:
```
프레임 폭 = 2 ÷ 0.000772 ≈ 2591 픽셀
프레임 높이 = 2 ÷ 0.001316 ≈ 1520 픽셀
```

### 정규화(Normalization) — 왜 필요한가

**문제**: 앱은 픽셀(0~2591)로 아는데, 웹 화면은 크기가 제각각(창을 줄이면 800px).

**해결**: **비율(0~1)로 바꿔서** 넘긴다.
```
정규화 = 픽셀좌표 ÷ 프레임크기
예) x=1563 ÷ 2591 = 0.603   → "화면 왼쪽에서 60.3% 지점"
```

그러면 웹은 화면 크기만 곱하면 끝:
```js
const x = o.box[0] * 캔버스폭;   // 0.603 × 800px = 482px
```
**화면 크기가 바뀌어도, 카메라 해상도가 바뀌어도 항상 정확히 맞는다.**

---

## 6. 움직임 감지 원리 (핵심 알고리즘)

### 아이디어

> 사진 여러 장을 놓고, **처음 것과 나중 것의 위치를 비교**해서 많이 움직였으면 "움직인다".

### 왜 바로 옆 프레임과 비교하면 안 되나

```
0.2초 간격 (옆 프레임)          1초 간격 (5프레임 전)
가만히 선 사람: 1003 → 1005     가만히 선 사람: 1003 → 1002
  차이 2px (AI 오차로도 이만큼   걷는 사람:    1563 → 1899
  흔들림) → 판단 불가              차이 336px → 확실히 움직임!
```

AI가 매 프레임 그리는 박스는 **미세하게 떨린다**(노이즈).
옆 프레임끼리 비교하면 가만히 있어도 떨림 때문에 "움직인다"고 오판한다.
→ **간격을 벌려서(5프레임 ≈ 1초)** 보면 진짜 이동만 남는다.

### 실제 동작

```
객체마다 최근 6프레임 좌표를 저장 (줄 세우기)

  [1563] [1600] [1650] [1700] [1750] [1899]
     ↑ 5프레임 전                      ↑ 지금
     └──────── 거리 계산 ─────────────┘
              336px

  336px > 임계값(78px)  →  "움직인다!" → 이벤트 발사
```

**임계값 78px은 어디서 나왔나?**
```
임계값 = 프레임폭 × 3% = 2591 × 0.03 ≈ 78px
```
고정 숫자가 아니라 **비율**로 계산 → 해상도가 달라져도 자동으로 맞춰짐.

### 3초 유지 (껌뻑거림 막기)

임계값 하나로 "지금 움직이냐"만 보면 경계선에서 **껌뻑거린다**:
```
78, 77, 79, 76, 80 px …  → 움직임 ON/OFF/ON/OFF/ON  ← 이벤트 폭탄!
걷다가 신호 기다리며 잠깐 멈춰도 → OFF 됐다가 다시 ON
```

그래서 **"한 번 움직이면 3초간은 계속 움직이는 것으로 친다".**
```
움직임 감지 → 🔴 ON + 타이머 = 지금+3초
   3초 안에 또 움직이면 → 타이머 리셋 (계속 🔴)
   3초 내내 조용하면    → 그때 OFF
```
잠깐 멈칫하거나 AI 좌표가 튀어도 3초 안에 다시 움직이면 계속 빨강 유지 → 안정적.
> 예전엔 "켜지는 기준(3%)/꺼지는 기준(1.5%)"을 다르게 둔 **히스테리시스**를 썼는데,
> **시간 기반(3초)이 더 직관적**이라 그걸로 바꿨다. (온도조절기·자동문도 히스테리시스 원리)

### 그리고 — 움직이는 것만 화면에 (B 방식)

가만히 있는 사람/차량은 **박스도 이벤트도 없다.** 오직 움직이는 것만 🔴 표시.
"객체가 나타났다"는 알림(`objectdetected`)도 없앴다 — 이 앱의 목적은 딱 **"진짜 움직이는 것"** 이니까.

### 객체를 어떻게 구분하나

WiseAI가 **`ObjectId`(번호표)** 를 붙여준다.
같은 사람은 프레임이 바뀌어도 같은 번호를 유지한다.
→ 우리는 그 번호로 **번호별 좌표 기록장**을 만들면 끝.

```cpp
tracks_[채널][ObjectId].centers  // 이 사람의 최근 6프레임 좌표
```

### Human만 거르는 이유

한 사람이 잡히면 메타데이터엔 **객체가 3개** 나온다:
```
ObjectId 1981  Human  ← 몸통 전체
ObjectId 1921  Head   ← 머리
ObjectId 1910  Face   ← 얼굴
```
전부 추적하면 **한 명당 이벤트 3번**. 그래서 `Human`(몸통)만 쓴다 → 사람 1명 = 박스 1개.

---

## 7. 웹페이지는 어떻게 영상 위에 네모를 그리나

### 층을 겹친다 (레이어)

```
   ┌─────────────────────┐
   │  <canvas>  네모 그림 │  ← 투명 유리판 (위)
   ├─────────────────────┤
   │  <img>     영상      │  ← 사진 (아래)
   └─────────────────────┘
        같은 위치에 겹침
```

CSS로 둘 다 `position:absolute; inset:0` 하면 완전히 포개진다.
캔버스는 배경이 투명이라 아래 영상이 비쳐 보인다.

### 두 개의 반복 루프

```
루프 A (150ms마다)         루프 B (500ms마다)
  /detections 요청           /snapshot 요청
       ↓                          ↓
  네모 다시 그리기            영상 갱신
```

**왜 분리했나?** 영상은 무거워서 느리다(초당 2장).
근데 네모는 가벼우니 자주 갱신할 수 있다(초당 6~7번).
→ **영상이 느려도 네모는 부드럽게 따라간다.**

### 그리는 코드

```js
objs.forEach(o => {
  const x = o.box[0] * W;          // 0~1 비율 × 캔버스 폭 = 실제 위치
  const y = o.box[1] * H;
  ctx.strokeStyle = o.moving ? '#ff3b5c' : '#31e281';  // 움직이면 빨강
  ctx.strokeRect(x, y, w, h);      // 네모 그리기
});
```

---

## 8. 빌드~설치가 무슨 과정인가

### 왜 도커가 필요한가

내 PC는 **x86** CPU, 카메라는 **ARM** CPU. 서로 기계어가 다르다.
→ 내 PC에서 만든 프로그램은 카메라에서 **안 돈다**.

**크로스 컴파일**: "내 PC에서, 카메라용 기계어를 만들기"
이걸 하려면 특수한 컴파일러/라이브러리 세트가 필요한데,
한화가 그걸 **도커 이미지**(`opensdk`)에 다 담아서 줬다.

```
내 PC ──> [도커 상자: 카메라용 컴파일러] ──> 카메라용 .so ──> .cap 포장
```

### 전체 과정

```
1. 코드 작성 (.cc, .h)
        ↓  docker compose up
2. 컴파일 → libsample_component.so   (카메라용 기계어)
        ↓
3. 패키징 → object_detect.cap        (설치 파일, 서명 포함)
        ↓  카메라 웹에서 업로드
4. 설치 → 카메라 안에 앱 복사
        ↓  [Start] 클릭
5. 실행 → 우리 컴포넌트가 살아나서 이벤트 받기 시작
```

`docker-compose.yml` 이 하는 일 (한 줄씩):
```yaml
cd /opt/object_detect/app && mkdir -p build && cd build
cmake -DSOC=cv5 ..        # cv5 = 이 카메라의 칩 종류
make clean && make        # 컴파일
make install              # 결과물을 libs/, res/ 로 복사
opensdk_packager -s cv5   # .cap 으로 포장 + 전자서명
```

---

## 9. 왜 어떤 건 되고 어떤 건 막히나 (보안 개념)

이 프로젝트에서 제일 많이 막혔던 부분. 웹 보안 3가지만 알면 이해된다.

### 9.1 인증 (Authentication) — "너 누구야?"

카메라는 아무나 못 보게 **로그인**을 요구한다.
**Digest 인증**: 비밀번호를 그대로 안 보내고, 암호화된 응답으로 신원 증명하는 방식.

브라우저는 한번 로그인하면 **기억**해뒀다가 자동으로 붙여준다.

### 9.2 동일 출처 (Same-Origin) — "같은 집이냐?"

```
http://192.168.0.5/home/...     ← 우리 앱 페이지
http://192.168.0.5/stw-cgi/...  ← 카메라 CGI
        ↑ 같은 IP, 같은 포트 = 같은 출처(Origin)
```
같은 출처면 보통 자유롭게 요청할 수 있다. **근데 우린 막혔다.** 왜?

### 9.3 요청 출처 검사 (Sec-Fetch / CSRF 방어) — "어디서 왔냐?" ⭐

브라우저는 요청할 때 **"이 요청이 어떤 상황에서 발생했는지"** 를 자동으로 알려준다.

```
주소창에 직접 입력        →  Sec-Fetch-Site: none      ("사용자가 직접 침")
페이지 안의 JS가 요청     →  Sec-Fetch-Site: same-origin  ("스크립트가 자동으로")
```

카메라는 **"스크립트가 자동으로 부른 요청"을 거부**했다. (58바이트 거부 텍스트 반환)
이건 **CSRF 공격**(악성 사이트가 몰래 내 카메라를 조작하는 것)을 막는 보안 장치다.

**핵심**: 이 헤더는 **브라우저가 강제로 붙인다. JS로 못 지운다.**
→ 그래서 `<img>`, `fetch`, `Accept` 헤더, `referrerpolicy` 다 시도해도 소용없었다.

### 9.4 그래서 어떻게 뚫었나 — 서버측 프록시

```
❌ 브라우저 ──직접──> 카메라 CGI
   (브라우저가 헤더를 붙임 → 차단)

✅ 브라우저 ──> 우리 앱 /snapshot ──> 카메라 내부 SDK
   (앱은 브라우저가 아님 → 헤더 없음 → 차단 없음)
```

**프록시(Proxy)** = 대신 가져다주는 중개자.
우리 앱은 **카메라 안에서** 도니까 브라우저 규칙을 안 받는다.
그래서 앱이 스냅샷을 떠서 브라우저에 넘겨준다.

> 이건 웹 개발의 **아주 흔한 정석 패턴**이다.
> "클라이언트에서 못 하면 서버가 대신 한다."

---

## 10. 우리 코드 구조 한눈에

```
object_detect/
├── README.md                 ← 무엇을 만들었나 (기술 문서)
├── BASICS.md                 ← 이 파일 (입문 가이드)
├── docker-compose.yml        ← 빌드 명령 모음
├── config/
│   └── app_manifest.json     ← 앱 이름/버전/권한
└── app/
    ├── html/
    │   └── index.html        ← 웹 UI (영상+네모 오버레이)
    ├── src/
    │   ├── PLifeCycleManagermanifest.json   ← 배선도 ①: 회사 설정
    │   └── sample_component/
    │       ├── sample_component.cc          ← ★ 우리 로직 전부
    │       ├── includes/sample_component.h  ← 변수/함수 선언
    │       └── manifests/
    │           └── SampleComponent_manifest_instance_0.json  ← 배선도 ②
    ├── libs/                 ← 컴파일 결과 (.so)
    └── res/models/           ← 배선도 사본 (빌드가 자동 복사)
```

### `sample_component.cc` 함수 지도

```
Initialize()            앱 시작 시 1번 — 로그 설정, HTTP 창구 열기
   │
ProcessAEvent()         모든 쪽지의 입구 — 종류 보고 분배
   ├─ HandleMetadataEvent()   메타데이터 쪽지 → XML 꺼내기
   │     └─ ProcessObjects()  ★ 파싱 + 움직임 판정 (핵심)
   │           └─ EmitEvent() 이벤트 출력 (로그 + 뷰어)
   └─ HandleHttpRequest()     웹 요청 쪽지 → /detections, /snapshot 응답
```

---

## 11. 이 프로젝트에서 배운 것 (면접에서 쓸 수 있는 이야기)

1. **문서보다 실제 데이터가 진실**
   → SDK 문서엔 C++ 구조체만 있어서 JSON인 줄 알았는데, 실제론 ONVIF XML이었다.
     raw 로그를 찍어보고서야 알았다.

2. **잘 도는 것과 diff 하는 게 가장 빠른 디버깅**
   → 앱이 죽었을 때, 공식 샘플(`metadata_sample`)과 설정을 1:1로 비교해서
     `{Ch}` 템플릿 차이를 찾아냈다.

3. **증상과 원인은 다르다**
   → `OpenAPIDispatcher not found` 예외가 무섭게 떴지만 무해했고,
     진짜 원인은 조용한 `EventQueue is not allocated` 였다.

4. **노이즈가 있는 신호는 시간 간격을 벌려서 본다**
   → 옆 프레임 대신 5프레임 전과 비교. 3초 유지 타이머로 껌뻑거림 제거.

5. **클라이언트에서 막히면 서버가 대신한다 (프록시 패턴)**
   → 브라우저 보안은 우회 대상이 아니라 **설계를 바꿀 신호**였다.

6. **정규화는 이식성을 만든다**
   → 좌표를 0~1로 바꿔 넘기니 해상도·화면크기와 무관해졌다.

---

## 12. 더 공부하면 좋은 것

| 주제 | 왜 |
|------|-----|
| **이벤트 기반 아키텍처** | OpenSDK뿐 아니라 대부분의 임베디드/서버가 이 구조 |
| **ONVIF 규격** | CCTV 업계 표준. 알아두면 어떤 카메라든 통함 |
| **객체 추적(Tracking)** | 우리는 WiseAI가 준 ID를 썼지만, 직접 하려면 IoU/칼만필터 |
| **HTTP & 웹 보안** | 인증/CORS/CSRF — 9장에서 겪은 것들의 정식 이론 |
| **크로스 컴파일 / 도커** | 임베디드 개발의 기본기 |

---

다음: [README.md](README.md) — 실제 구현 상세와 트러블슈팅 전체 기록

# 번호판 숫자 인식(OCR) 개발 여정 — 완전판 공부 노트

> 카메라(엣지) 안에서 한국 번호판 **숫자를 읽어내기까지**의 전 과정.
> 결과: **온카메라 1샷 인식율 0% → 86%** (프레임당 40~60ms, 앱 가용 2코어)
> PC 예측(86%)과 카메라 실측(12/14=86%)이 **정확히 일치** — 실험→포팅→검증의 교과서적 사이클.

---

## 0. 한눈에 보는 최종 성적

| 지표 | 값 |
|---|---|
| 온카메라 1샷 인식율 | **86%** (12/14, conf 1.00 다수) |
| 처리 시간 | 크롭당 40~60ms (모델추론 자체는 3~5ms) |
| 모델 크기 | tinyLPR 87KB + Multi-line 543KB |
| 악명높던 `36라7833` | **conf 1.00으로 정복** (범용 OCR은 전부 실패했던 글자) |
| 시간투표(차량단위) | PLATE FINAL로 자가교정까지 확인 |

최종 파이프라인:
```
WiseAI 크롭(헐렁) → [크기필터 300px] → 후보 6~8개 생성 → tinyLPR 전수판독
→ 유효포맷>신뢰도 선택 → Multi-line 병합 → good-shot 2표+버스트샘플 시간투표
→ PLATE FINAL 확정
```

---

## 1. 문제 정의와 제약

**목표**: 주차관리 시나리오 — 카메라 앞을 지나는 차의 번호판 **텍스트**를 자동 추출.

**출발점의 벽**:
- WiseAI(카메라 내장 AI)는 번호판 **박스 위치**와 **크롭 이미지**는 주지만 **숫자는 절대 안 줌** (OpenSDK 앱에 미공개)
- 카메라 NPU에 커스텀 모델 올리기 → Ambarella CVflow 툴체인이 없어 **불가**
- Hanwha 자체 LPR 앱(WiseParkingGuidance) → 설치 불가/.cap 미입수
- 남은 길: **CPU 추론** (앱 가용 코어 2개, OpenCV는 이미 정적링크 확보)

**설계 원칙** (이전 리팩터링에서 이어짐):
- 실험은 PC에서 빠르게 → 검증된 것만 카메라 C++로 이식
- OCR은 교체 가능한 블록(인터페이스)으로 — 모델이 바뀌어도 파이프라인 불변

---

## 2. Phase A — PC 실험실 (ocr_lab)

실측 크롭 101장(고유 번호판 12종: 흰색 8 + 파란EV 2 + 연두 2), 정답은 육안 라벨(labels.csv).
모든 스크립트가 `from gt import GT`로 정답 공유 — 라벨 한 곳 관리.

### A-1. 범용 OCR 3종 비교 → "한글이 병목"

| 모델 | 구조 | 완전일치 | 교훈 |
|---|---|---|---|
| PaddleOCR(det=True) | DBNet검출+SVTR인식 | 66% | 검출을 끄면(det=False) 배경까지 읽어 0% — **설정이 성능을 좌우** |
| EasyOCR(ko) | CRAFT+CRNN | 41% | 숫자는 맞는데 한글 오인식(머→대, 서→사) |
| Tesseract(kor) | LSTM | 0% | 문서용 엔진은 번호판 폰트에 무력 |

**핵심 발견**: 오답의 대부분이 **번호판 전용 한글 40자**(가·나·머·서·소·라…)에서 발생.
범용 OCR은 이 특수 폰트를 학습한 적이 없다.

### A-2. 이미지 개선 10종 → "병목은 블러가 아니라 노이즈"

| 기법 | 결과 | 교훈 |
|---|---|---|
| denoise(fastNlMeans) | 66→**77%** | 저조도 노이즈 제거가 최고 효자 |
| bilateral | 71% | 엣지 보존형도 유효 |
| CLAHE | EasyOCR엔 +12%p, Paddle엔 무효 | **모델마다 약이 다르다** |
| unsharp 샤프닝 | 47% (악화) | 노이즈·압축 아티팩트까지 증폭 |
| **Wiener 모션디블러** | 26%→0% (대실패) | 이론상 맞아도 링잉이 글자를 파괴. **데이터가 이론을 이긴다** |
| 업스케일(2~4배) | 악화 | 픽셀을 늘려도 정보는 안 늘어남 |

### A-3. 해상도 분석 → "500px 임계값"

| 크롭 폭 | 인식율 |
|---|---|
| ~500px | **0%** |
| 500~600px | 82% |
| 600px~ | 92% |

멀리 있는 차의 작은 크롭은 어떤 기법으로도 못 살림 → **크기 필터**라는 공학적 결론.

### A-4. 투표 기법들 → "범용의 천장은 88%"

- **앙상블**(Paddle+EasyOCR×전처리 = 6표 자리별투표): 82%
- **TTA**(한 장을 denoise/CLAHE/확대 3변형으로 자가투표): 78%
  - ⚠ 같은 이미지를 그냥 N번 돌리면 항상 같은 답(모델은 결정론적) — **"다른 변형"이라야 표가 갈린다**
- **자리별 투표(positional vote)**: 통짜 다수결과 달리 글자 자리마다 따로 다수결
  - `[숫자2~3][한글1][숫자4]`로 분해 → 자리별 최다 글자 조립
  - 통짜는 한 글자 틀린 표를 통째로 버리지만, 자리별은 맞은 자리를 재활용
- **시간투표**(같은 번호판 여러 캡처): 차량단위 88~100%

**결정적 한계 증명**: `36라7833`을 30번 인식(10프레임×3변형)했는데 정답 `라`가 **0표**.
투표는 "후보에 등장한 것"만 뽑을 수 있다 → 모델이 근본적으로 못 읽는 글자는 **어떤 투표로도 복원 불가**.
→ 범용 OCR + 모든 후처리의 천장 = 82~88%. **모델 자체를 바꿔야 한다.**

### A-5. 전환점 — tinyLPR 발견

`noahzhy/KR_LPR`의 **tinyLPR**: 한국 번호판 **전용으로 이미 학습된** TFLite 모델.

| 항목 | 스펙 |
|---|---|
| 크기 | **87KB** (imagenet류 모델의 1/1000) |
| 속도 | PC 1.4ms / 카메라 3~5ms |
| 입력 | 192×96 그레이스케일 |
| 디코딩 | CTC |
| 라이선스 | GPL-3.0 (프로젝트용 OK, 상용이면 자체학습 필요) |

**첫 테스트의 반전**: 우리 크롭 그대로 넣으니 **22%** (범용보다 못함!).
원인 분석 → tinyLPR은 **번호판이 화면을 꽉 채운 이미지로 학습**됨.
WiseAI 크롭은 번호판+범퍼+배경이 섞인 "헐렁한" 크롭 → 모델 입장에선 글자가 작고 어긋남.

검증: Paddle 검출로 번호판만 딱 잘라(타이트크롭) 넣으니 → **92%**. 
"모델이 나쁜 게 아니라 **입력 프레이밍이 문제**"라는 진단 확정.

같은 저자의 **Multi-line 모델**(96×32, 2줄 대응)도 확보: 단일줄 88%, 속도 0.7ms.
→ 두 모델은 서로 다른 걸 틀림(상호보완 12건) — 나중에 병합 재료가 됨.

### A-6. 멀티후보 트리밍 발명 — 이 프로젝트의 핵심 아이디어

**문제**: 타이트크롭이 필요한데, PC에서 쓴 Paddle 검출(39ms+무거움)은 카메라 2코어에 부적합.
순수 OpenCV 휴리스틱(밝은영역 41%, 엣지밀도 17%)은 정확도 부족.

**발상 전환**: "정확하게 한 번 자르기"를 포기하고, **대충 자른 후보 여러 개를 모델에게 다 보여주고 고르게 하자.**
tinyLPR이 4ms밖에 안 하니까 8번 돌려도 32ms — 이 속도가 아이디어를 가능케 함.

```
후보 생성 (6~8개):
  ① 원본 그대로 (보험)
  ② Otsu 이진화→가로 모폴로지 닫기→번호판 비율(1.5~8:1) 사각형 top2 × 마진 2종
  ③ 중앙크롭 3종 (WiseAI 크롭은 번호판이 중앙에 오는 경향)
선택 규칙:
  유효 번호판 포맷([숫자2-3][한글][숫자4]) 우선 → 그중 신뢰도(confidence) 최고
```

**왜 다수결이 아니라 신뢰도인가**: 후보 대부분은 잘못 잘린 쓰레기(다수결하면 쓰레기가 이김).
잘 잘린 1~2개에서 모델이 **높은 확신**을 보이므로, 확신도가 심판 역할.

**결과: 17% → 86%** (101장). 2-pass(재귀 크롭)·Multi-line 후보 추가 실험 → 86%에서 포화 = 공간축 레버 소진 확인.

### A-7. 신뢰도(confidence)라는 무기

- 계산: CTC 출력에서 blank가 아닌 timestep들의 softmax 확률 **기하평균**
- 용도 3가지:
  1. 후보 크롭 선택의 심판
  2. 두 모델 불일치 시 타이브레이커 (같으면 채택→한쪽만 유효포맷이면 그쪽→둘 다 유효면 conf 승자)
  3. 버스트 환각 필터 (아래 C-3)
- **한계도 실측**: 모델이 "틀린 답을 0.98로 확신"하는 과신 사례 존재 → 신뢰도는 만능 아님, 규칙의 한 겹일 뿐

---

## 3. Phase B — 카메라 이식 (진짜 엣지 배포)

### B-1. 1차 시도: ONNX + OpenCV dnn → 실패 (교훈 큼)

계획: TFLite→ONNX 변환(tf2onnx) 후 이미 링크된 OpenCV의 dnn 모듈로 추론.
결과:
- **tinyLPR**: 모델 내부 `DynamicQuantizeLinear`(동적양자화 연산)를 OpenCV dnn이 미지원 → **로드 자체 불가** (--dequantize 옵션으로도 안 사라짐)
- **Multi-line**: 로드는 되는데 정확도 88%→10% 붕괴 (변환 과정의 연산 의미 차이)

**교훈**: 모델 포맷 변환은 "파일이 만들어졌다 ≠ 같은 모델이다". 변환 후 **원본과 출력 대조 검증**이 필수며, 이 검증 단계 덕분에 카메라에 올리기 전에 실패를 조기 발견했다.

### B-2. 2차 시도: TFLite C 런타임 직접 크로스컴파일 → 성공

전략: 변환을 포기하고 **원본 TFLite를 그대로 돌릴 런타임을 카메라용으로 빌드**.

1. 카메라 툴체인 확인: `cortex-a76 gcc12.1 aarch64-linux-gnu` (표준 glibc — 다행)
2. Docker(ubuntu22.04 + `gcc-12-aarch64-linux-gnu`)에서 TensorFlow 소스의 **`tensorflow/lite/c`를 CMake로 configure** — 핵심 함정: `tensorflow/lite`가 아니라 **`lite/c`가 별도 CMake 프로젝트**라서 그걸 소스 디렉토리로 지정해야 `tensorflowlite_c` 타겟이 생김
3. 산출물: `libtensorflowlite_c.so` (6.4MB, XNNPACK 활성)
4. **C API를 선택한 이유**: extern "C" 경계는 컴파일러가 달라도 C++ ABI 문제(이름 맹글링, STL 레이아웃)에서 자유 — 크로스컴파일 조합에서 안전
5. 배포: `.so`를 `libs/sample_component/`에 함께 설치 + **rpath=$ORIGIN** ("나와 같은 폴더에서 라이브러리를 찾아라") → .cap에 그대로 패키징

빌드 중 삽질 기록:
- docker build 스텝에 `| tail`을 붙였다가 **파이프가 실패 exit code를 삼켜** "성공한 척"한 빌드 발견 (`set -o pipefail` 교훈)
- 의존성 캐시 재활용 시도 → FetchContent 경로 충돌로 실패 → 깨끗한 재빌드가 정답이었음

### B-3. C++ 추론 모듈 (신규 4파일)

| 파일 | 역할 |
|---|---|
| `includes/ocr/tflite_model.h` | TFLite C API RAII 래퍼 (Load/InputHW/Run). NHWC/NCHW 자동 판별 — 채널=1이면 메모리 배치가 동일하다는 점 활용 |
| `includes/ocr/plate_decode.h` | CTC collapse(연속중복+blank 제거), 신뢰도(기하평균), 번호판 포맷 검증(UTF-8 바이트 파싱: 숫자 1B/한글 3B) |
| `includes/ocr/plate_ocr.h` | 멀티후보 생성(cv::threshold/morphologyEx/findContours — Python과 파라미터 동일) + 전수판독 + 신뢰도 병합. 단계별 ms 계측 |
| `includes/ocr/plate_vote.h` | 시간투표: oid별 샘플 누적(스로틀 250ms·상한 8), 자리별 다수결(PositionalVote) |

연결부: `PlateStore::Save()` 성공 직후 `RecognizePlate(ch, slot)` 호출 → 이벤트 발행(단계별 타이밍 포함) + `/platetext?ch=N` JSON.

### B-4. 실전 디버깅 일지 (카메라에서 겪은 것들)

| 증상 | 원인 | 해결 |
|---|---|---|
| 뷰어 연결 안 됨(stoi 예외) | gdbserver_control manifest가 placeholder IP | 두 도구 모두 IP 기입 + 실행 순서(gdb→viewer) |
| 뷰어 이벤트 침묵 | 좀비 뷰어가 포트 점유(bind error 98) | pkill 후 단일 인스턴스 |
| 인식률 17~30% | 예측된 "헐렁 크롭" 상태 | 멀티후보 C++ 포팅 → conf 1.00 러시 |
| 크롭이 갑자기 개판 | ①영상 1.25배 재생(모션블러↑) ②카메라가 모니터를 비스듬히 봄(사다리꼴 왜곡) | 1배 재생 + 정면 재배치. **앱은 픽셀을 안 건드림 — 원인은 항상 상류에** |
| 유령 번호판 저장 | 144·272px 초소형 크롭에서 환각 | 크기 필터(폭<300px 스킵) — A-3의 실측이 근거 |

### B-5. 시간축 버스트 + 시간투표 (마지막 레버)

**배경**: WiseAI good-shot은 차 1대당 1장 → 1샷 86%가 끝. 그 위로 가려면 샘플을 늘려야 함.
"good-shot 3장 요청" 같은 API는 없음 → **우리가 직접 만든다**:

```
번호판 추적 중 (bbox가 매 프레임 옴 — 이미 수신 중)
→ eAppSnapshotJpeg(전체화면, 라이브뷰에 쓰던 것) 반복 요청
→ 스냅샷에서 bbox+여백(가로35%/세로60%)으로 직접 크롭
→ 멀티후보 OCR (여백 부정확은 멀티후보가 흡수 — 두 기능의 궁합)
→ oid당 샘플 누적 → 추적 종료 시 자리별 다수결 → PLATE FINAL
```

**v1 실패**: 1.25배 고속 재생에서 스냅샷(수백 ms 전 화면)과 bbox가 어긋남 → 크롭에 번호판이 없는데 tinyLPR이 conf 0.5~0.85짜리 **환각 번호판**을 생성 → 투표 오염(유령 FINAL).

**v1 로그가 준 결정적 단서**: 환각은 conf 0.5~0.85, 진짜는 0.93+ — **신뢰도 분포가 갈라져 있었다.**

**v2 수정**:
1. 버스트 수락 기준 0.5 → **0.90** (환각 원천 차단)
2. **good-shot 결과를 가중 2표**로 투표 합류 (가장 선명한 순간이 투표 주도)
→ 결과: 환각 소멸, FINAL 정확, 심지어 **자가교정 목격** (버스트만으로 "23누5701" 확정 후 good-shot 합류로 "25누5701" 재확정).
1배속(실제 주차장 속도 근사)에선 버스트 샘플이 **conf 1.00 정답**으로 잡히기 시작 — 시간축 실증.

---

## 4. 공부 포인트 — 핵심 개념 사전

**CRNN / CTC**
- CRNN = CNN(시각특징) + RNN(순서) + CTC(정렬). OCR 인식의 표준 구조
- CTC(Connectionist Temporal Classification): 글자 위치 라벨 없이 가변길이 텍스트 학습.
  디코딩 = timestep별 argmax → 연속 중복 접기 → blank 제거. `-15-소-소--5...` → `15소5...`

**신뢰도(confidence)**
- 비-blank timestep들의 최고확률 기하평균. "모델이 얼마나 확신했나"
- 산술평균 대신 기하평균: 한 글자라도 확률이 낮으면 전체가 확 깎임 (약한 고리 반영)

**Test-Time Augmentation(TTA)**
- 추론 시 입력을 여러 변형으로 만들어 결과를 합치는 실무 기법
- 멀티후보 트리밍은 TTA의 공간(크롭) 버전이라 볼 수 있음

**자리별 투표(positional voting)**
- 문자열 통짜가 아니라 자리(slot)마다 독립 다수결 → 부분적으로 맞은 샘플도 기여
- 한계: 후보에 한 번도 안 나온 글자는 어떤 투표로도 못 만든다 (A-4의 `라` 증명)

**모델 신뢰도의 함정 (환각)**
- 모델은 입력에 번호판이 없어도 "그럴듯한 번호판"을 출력한다 (CTC는 항상 뭔가 뱉음)
- 방어: 신뢰도 임계값 + 포맷 검증 + (실전) 등록DB 대조

**크로스컴파일과 ABI**
- 타겟(카메라)용 바이너리를 호스트(PC/도커)에서 빌드
- C++ 라이브러리는 컴파일러 버전이 다르면 ABI 충돌 위험 → **C API(extern "C")** 경계가 안전판
- rpath=$ORIGIN: 실행파일/so가 "자기 위치" 기준으로 의존 so를 찾게 하는 링커 설정

**엣지 AI 설계 감각**
- "모델 1개 크게" 대신 "작은 모델 × 여러 번 + 영리한 규칙"이 엣지에선 강하다
  (tinyLPR 87KB를 8번 돌리는 게 Paddle 1번보다 10배 빠르고 더 정확했다)
- 벤더 기능(WiseAI 크롭)은 버리지 말고 **그 위에 한 겹 얹는다** (검출은 공짜로 받고 트리밍만 추가)

---

## 5. 실패 카탈로그 (성공만큼 중요한 기록)

| 실패 | 왜 실패했나 | 뭘 배웠나 |
|---|---|---|
| Wiener 디블러 | 커널 어긋남 → 링잉이 글자 파괴 | 이론적 우아함 ≠ 실측 성능 |
| 샤프닝/업스케일 | 아티팩트 증폭 | 손실된 정보는 후처리로 안 돌아옴 |
| Paddle det=False | 배경까지 한 줄로 우겨 읽음 | 라이브러리는 "올바른 모드"로 써야 함 |
| 3-way 다수결(약체 포함) | 69%짜리 Paddle이 정답을 뒤집음 | 약한 투표자는 빼는 게 낫다 |
| 색상 라우팅 | 규칙을 쌓아도 순수 신뢰도와 동률 | 병합 규칙의 한계 = union(상한) |
| ONNX 변환 | 미지원 연산/의미 변형 | 변환 후 반드시 출력 대조 검증 |
| 버스트 v1 | 시간 어긋남 + 환각 + 무른 필터(0.5) | 로그의 신뢰도 분포가 해답을 알려줌 |
| 2-pass 크롭 | 86%에서 포화 | 한 축의 레버는 끝이 있다 — 다음 축으로 |

---

## 6. 남은 과제

1. `42주0120` 계열 오답 (0↔7, 주↔조) — 실전에선 등록차량 DB 최근접 매칭으로 보정하는 영역
2. 실차 환경 검증 (지금까지는 모니터 재생 — 모아레/블러 최악 조건이었음)
3. 상용화 시: tinyLPR(GPL-3.0) 대신 합성데이터(qjadud1994 생성기)로 자체 모델 학습
4. 구형 지역명/2줄 번호판 — Multi-line 모델 활용 여지

## 7. 재현 방법

```bash
# PC 실험 (WSL, Python 3.12 venv — 3.14는 paddle/torch wheel 없음)
cd /mnt/c/Users/3-28/Desktop/rtsp/ocr_lab && source .venv/bin/activate
python fetch_crops.py --password '***' --count 300   # 카메라에서 크롭 수집
python make_sheet.py                                  # 라벨링 시트
python candidates_test.py                             # 멀티후보 86% 재현
python battle.py                                      # tinyLPR vs Multi-line

# TFLite aarch64 런타임 빌드 (1회)
docker build -t tflite-aarch64-build .                # scratchpad/tflite_build/Dockerfile
# → build_capi2에서 tensorflow/lite/c 를 configure, tensorflowlite_c 타겟 빌드

# 카메라 앱 빌드
cd /mnt/c/Users/3-28/Desktop/rtsp/sdk/object_detect
APP_NAME=object_detect SDK_VER=26.05.19 SOC=cv5 docker compose up   # → object_detect.cap

# 확인
curl --digest -u admin:*** http://192.168.0.5/opensdk/object_detect/platetext?ch=2
# 디버그 뷰어: gdbserver_control → remote_debug_viewer (좀비 프로세스 pkill 주의)
```

## 8. 산출물 지도

```
object_detect/
├ app/src/sample_component/includes/ocr/     ← OCR 모듈 4파일 (신규)
├ app/src/sample_component/ocr_models/       ← 모델 4파일 (tinyLPR·Multi-line + labels)
├ app/3rd-party/tflite/                      ← 크로스컴파일한 런타임(.so) + 헤더
└ OCR_JOURNEY.md                             ← 이 문서

ocr_lab/  (PC 실험실)
├ labels.csv + gt.py                         ← 정답 단일 관리
├ compare_ocr.py / preprocess_experiment.py / size_analysis.py
├ ensemble*.py / *voting*.py / confidence_merge.py / color_route_merge.py
├ tinylpr_test.py / tinylpr_tight.py / multiline_test.py / battle.py
├ candidates_test.py / candidates_duo.py / candidates_2pass.py   ← 멀티후보 시리즈
├ photo_tta.py / photo_tta2.py / variant_select.py / beam_decode.py  ← 광학 TTA·디코딩 실험
├ 번호판OCR_실험보고서.md / .html            ← 보고서(표·차트)
└ *_check.html                               ← 눈검증 갤러리들
```

---

## 9. 2026-07-27 업데이트 — "이번 주" 4카드 실행 결과

### 9-1. CTC 빔서치 + 포맷제약 → 기각 (0건 구제)

`beam_decode.py`: prefix beam search(빔16) 로 그리디 vs 빔top-1 vs 빔+포맷제약 비교 → **87% = 87% = 87%**.
tinyLPR 출력 분포는 아주 뾰족해서(맞을 때도 틀릴 때도 0.99 확신) 2등 경로에 정답이 없다.
**교훈: 오답은 디코딩 실수가 아니라 모델 확률 자체의 오류 → 디코더 개선은 헛수고, 답은 입력 화질/모델.**

### 9-2. 시간투표 → "신뢰도 챔피언십"으로 재설계 (버스트 승격)

- 기존: good-shot 2표 다수결 → 실측 FINAL≡good-shot(무의미)
- 신규(`plate_vote.h`): 샘플=(텍스트, conf, 출처). **유효포맷 중 conf 최고가 승리.**
  good-shot(Q45 JPEG)이 블러/저화질일 때 스냅샷 직접크롭(버스트)이 역전 가능.
- **실전 구조 확인**: 블러 런에서 good-shot "555소6446"(0.81) 을 버스트 "15소5746"(1.00) 이 역전 → FINAL 정답. 2건.
- 조기확정 방지: good-shot 크롭은 최대 3초 늦게 써짐(RetryPending) → primary 샘플 없으면 stale 유예 3배(15→45프레임).
  (수정 전 실측: 버스트 환각 2개가 ≥2 가드를 뚫고 오답 FINAL 선발사 — id9595)

### 9-3. Multi-line 제거 (경량화)

어블레이션 + 실전 로그 모두 기여 0 (픽을 못 따내며 판마다 3~15ms 소모, 오히려 "전남3바83" 류 잡음).
유일한 실전 구제 1건(42주0120@304px)도 줌 개선 후 tiny 단독 1.00 으로 대체됨.
→ 모델 로드·추론·병합규칙 5분기·패키징 전부 삭제. .cap 12.4→11.9MB, 파이프라인 단일 경로.

### 9-4. AE 셔터 튜닝 — 화질 축(①)의 첫 실측 성과 ★

- 증상: 갑자기 블러 심화, 오독이 "**227**바8857, **155**자1999, **555**소6446" 패턴 = **앞자리 겹침**
- 진단: 가로 모션블러의 CTC 지문 (글자가 노출시간 동안 흘러 같은 글자를 두 스텝이 봄)
- 처방: 노출 보정에서 **사용자 선호 셔터 = 최소 셔터 = 1/500** 고정 (밝기 부족은 게인/조명으로)
- 결과: **동일 세트 71%(10/14) → 100%(9/9), good-shot 거의 전부 conf 1.00**
- 부산물: 신뢰도 분포가 갈라짐 — 진짜 0.97~1.00 vs 버스트 환각 0.91~0.94.
  화질이 잡히면 **conf 0.95~0.96 컷으로 환각/진짜 완벽 분리 가능** (블러 런에선 겹쳐서 불가)

### 9-5. 진단 추가

`/lsdownload?ch=N` — /tmp/download/chN 파일 나열(이름·크기·mtime). 카메라가 oid 당 크롭을 몇 장 쓰는지 확인용.

### 갱신된 교훈 요약

1. **화질(캡처) > 후처리**: 셔터 1단 조정이 빔서치·TTA·모델병합 전부를 합친 것보다 컸다.
2. **신뢰도는 화질이 좋을 때만 믿을 수 있는 심판이다** — 블러 상태에선 환각도 0.96까지 침.
3. 겹침 오독(227, 555)을 보면 모션블러를 의심하라 — CTC의 블러 지문.

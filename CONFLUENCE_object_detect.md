# object_detect · 움직이는 차의 번호판을, 카메라 안에서 잡아 저장하기

> **OBJECT_DETECT · EDGE APP** — PNM-C16083RVQ · cv5 · SDK 26.05.19
> Hanwha OpenSDK · 온디바이스 번호판 캡처

카메라 밖 서버 없이, **카메라 자체가 돌리는 앱(edge)** 이 지나가는 차량의 번호판 이미지를 포착해 저장한다. 이 문서는 그 앱이 어떻게 동작하는지와, 거기까지 오며 무엇을 시도하고 왜 지금 구조로 정착했는지를 top-to-bottom으로 정리한 학습 노트다.

| 구분 | 담당 | 내용 |
|---|---|---|
| **런타임** | 카메라 내부(edge) | 앱이 카메라 SOC 안에서 실행 |
| **감지엔진** | 카메라 WiseAI | 객체 감지 · good-shot |
| **번호판 크롭** | 카메라 ImageRef | 카메라가 잘라 파일로 저장 |
| **우리 몫** | 앱 | 받아서 저장 + 표시 |

---

## 01 · 목표 — 무엇을 만드는가

**한 문장으로:** 지나가는 차의 번호판을 읽을 수 있게 1장 저장.

주차장 입구 카메라 앞을 차가 지나갈 때, 그 차의 번호판 이미지를 캡처해서 카메라 안에 저장한다. 검증 단계에선 지나간 차마다 갤러리에 쌓아 눈으로 확인한다.

> ℹ️ 번호 **텍스트**는 이 카메라가 앱에 안 주므로 대상 밖 — **이미지까지가 목표**다.

| 단계 | 내용 |
|---|---|
| **입력** | 카메라 WiseAI 메타데이터 — 지나가는 객체(사람·차량·번호판) 정보 스트림 |
| **처리** | 번호판 크롭 파일을 받아 검증·저장 (우리 앱은 무거운 연산 안 함) |
| **출력** | 저장된 번호판 JPEG + 웹 갤러리로 확인 |

---

## 02 · 큰 그림 — 세 주인공

**무거운 일은 전부 카메라가 한다. 우리는 받아서 정리한다.**

1. **카메라 WiseAI** — 감지 · 크롭 · good-shot
   움직이는 차 감지 → 번호판 감지 → 제일 잘 찍힌 순간을 잘라 파일 저장
2. **우리 앱 (edge)** — 받아서 저장
   메타데이터에서 크롭 경로 뽑아 → 파일 읽어 → 디스크에 저장
3. **브라우저** — 꺼내서 표시
   저장분을 HTTP로 받아 갤러리로 보여줌 (검증용)

> ℹ️ **핵심 통찰** — 감지·최적프레임선택·크롭 같은 무거운 AI 연산은 전부 카메라(WiseAI)가 이미 한다. 우리 앱은 그 결과물(잘라둔 이미지)을 받아 **저장만** 하면 된다 → 가볍고 안정적.

---

## 03 · 캡처 파이프라인 — 4단계

**이 순서가 이 앱의 전부다.**

### 1단계 — 카메라가 번호판을 잘라 파일로 저장 `카메라`

WiseAI가 번호판을 감지하면 good-shot 엔진이 "제일 잘 찍힌 순간"을 판단해 그 부분만 잘라 카메라 내부(램디스크)에 저장하고, 그 경로를 메타데이터에 넣어준다.

```text
/tmp/download/ch0/objectid_922_1784605590786.jpg
                        └객체id┘  └──타임스탬프──┘
```

차가 다가오면 더 좋은 순간에 새 파일로 갱신 → 같은 객체id, 타임스탬프만 바뀜.

### 2단계 — 앱이 매 프레임 XML을 읽어 경로를 뽑음 `우리 앱`

카메라는 초당 여러 번 ONVIF XML 메타데이터를 이벤트로 보낸다. `ProcessObjects()`가 그때마다 — 진짜 XML 파서 대신 문자열 `find()` 스캔으로 번호판 객체와 그 `<tt:ImageRef>` 경로를 뽑는다.

```cpp
size_t pl = xml.find(">LicensePlate<", id_start);  // 번호판 객체?
size_t ir = xml.find("<tt:ImageRef>", id_start);    // 크롭 경로
iref = xml.substr(ir+13, e-(ir+13));                // "/download/ch0/..jpg"
```

> ℹ️ **ImageRef = Image Reference** — 이미지 데이터가 아니라 "위치(경로)"다. 사진은 디스크에, XML엔 작은 경로 문자열만 → 매 프레임 통째 사진 넣으면 무거우니까. 우리는 그 경로로 찾아가서 읽는다.

### 3단계 — 파일을 읽어 그대로 저장 `우리 앱`

경로에 `/tmp`를 붙여 실제 파일을 읽고, 완성된 JPEG(`FF D8 … FF D9`)인지 확인한 뒤 **바이트 그대로** 디스크에 저장한다. (디코드·반전·인코드 없음 → 순수 파일 I/O)

```text
read /tmp/download/ch0/objectid_922_..jpg
if (FF D8 … FF D9 완성?) → 아직 쓰는 중이면 pending 재시도
write ../storage/cap_N.jpg      // N = 0~99 순환버퍼
write ../storage/plate_last.jpg  // 최신 1장
```

좌우반전 교정은 저장 때 안 하고 **표시(브라우저 CSS)** 에서 → 3단계가 초경량.

### 4단계 — 브라우저 갤러리가 꺼내서 보여줌 `브라우저`

웹 페이지 JS가 `/platelist`로 저장 개수를 확인하고, 늘었을 때만 새 이미지를 `/plate?n=N`으로 받아 앞에 추가한다. `transform:scaleX(-1)`로 거울상을 똑바로 표시.

```js
if (total === lastTotal) return;   // 안 늘면 아무것도 안 받음
// 늘어난 것만 <img src=/plate?n=slot> 앞에 추가 (기존 재다운로드 X)
```

---

## 04 · 핵심 개념 사전

**이 앱을 이해하려면 알아야 할 용어들.**

| 용어 | 뜻 |
|---|---|
| **WiseAI** | 카메라 내장 AI 엔진. 움직이는/새로 나타난 객체를 감지. 정지한 주차 차량은 잘 안 보냄 → 결과적으로 "움직이는 차만" 잡히는 이유. |
| **ONVIF 메타데이터** | 객체 정보를 담은 XML 스트림 (JSON 아님). `<tt:Object>`·`<tt:Type>`·`<tt:ImageRef>` 태그로 구성. |
| **good-shot 엔진** | 카메라 내부 BestshotManager. 객체가 제일 잘 찍힌 순간을 스스로 골라 크롭 저장 + 갱신. 우리가 선명도 계산할 필요를 없애줌. |
| **ImageRef** | good-shot 크롭이 저장된 파일 경로 문자열. `/download/…` → 앱에선 `/tmp/download/…`로 읽힘. |
| **OpenSDK 구조** | 앱 = 컴포넌트 + 이벤트 + 매니페스트. 메타데이터는 MetadataManager가 "System" 채널로 보내줌 (배선 필수). |
| **순환버퍼** | `cap_0~99.jpg` 100칸을 돌려쓰기(N = 개수 % 100). 저장이 쌓여도 공간 안 참. |

---

## 05 · 감지 이벤트 & 핸들러

**움직임을 스스로 판정 → 움직이는 것만 이벤트를 낸다.**

이 앱은 "무엇이 움직이는가"를 직접 계산한다. 그 판정이 두 가지를 좌우한다 — 어떤 이벤트를 낼지, 그리고 화면에 어떤 박스를 그릴지. 특히 번호판은 "움직이는 차량"의 것만 의미를 갖는다.

### 움직임 판정 — 어떻게 "움직인다"고 아는가

1. **궤적 추적** — 객체(id)별로 최근 6프레임 중심좌표를 큐에 쌓는다.
2. **이동량 비교** — 지금 vs 5프레임 전 거리 > 좌표스케일×3% → "움직임".
3. **3초 홀드** — 한 번 움직이면 3초간 유지 — 잠깐 멈춰도 안 끊기게.

### 내보내는 이벤트 & 핸들러

| 이벤트 | 트리거 (언제) | 핸들러가 하는 일 |
|---|---|---|
| **움직이는 사람 감지** | 사람이 정지→움직임 전환하는 순간 (전환마다 1회) | EmitEvent → "움직이는 사람 감지! (chN idM)" |
| **움직이는 차량 감지** | 차량이 정지→움직임 전환하는 순간 | EmitEvent → "움직이는 차량 감지! (chN idM)" |
| **번호판 감지** | 새 번호판 객체가 처음 등장 | EmitEvent + 저장 시도 → "번호판 감지! … imgref=O" |
| **번호판 저장** | 크롭 파일을 디스크 저장 완료 | 파일 write + EmitEvent → "번호판 저장 #N (총M)" |

> ℹ️ **핸들러의 정체 = EmitEvent** — 지금 이 이벤트들의 핸들러는 전부 `EmitEvent → SendTargetEvents`로 remote_debug_viewer(PC)에 메시지를 쏘는 것. 앞으로 "움직이는 사람" 이벤트를 알람 출력·녹화·푸시알림 같은 다른 핸들러로 라우팅하려면 **바로 이 지점에서 갈라내면** 된다. (그 핸들러 설계는 별도 주제)

### 받는 이벤트 — 프레임워크 (`ProcessAEvent` 분기)

| 이벤트 타입 | 어디서 옴 | 핸들러 |
|---|---|---|
| `eMetadataRequest` | MetadataManager | HandleMetadataEvent → ProcessObjects |
| `eHttpRequest` | 브라우저/웹페이지 | HandleHttpRequest (엔드포인트 분기) |
| `eInformAppInfo` | LifeCycleManager | app_id 저장 (스냅샷 요청용) |

---

## 06 · 우리가 걸어온 길 — 문제 → 해결

**지금 구조는 여러 막다른 길 끝에 정착한 것.**

| # | 문제 | 시도/해결 |
|---|---|---|
| ❌ **문제** | 스냅샷으로 크롭 → 딜레이·어긋남 | 앱이 스냅샷(초당 2~3장, ~300ms 낡음)을 떠서 번호판을 직접 잘랐다. 움직이는 차는 크롭이 번호판을 빗나감. |
| 🔧 **시도** | 모션보정·풀해상도 확정크롭 | 속도로 박스 보정, 위쪽 패딩 등. 개선됐지만 근본 한계(느린 스냅샷)는 못 넘음. |
| ❌ **문제** | raw 30fps 스트림 시도 → 앱이 죽음 | ① 풀프레임 변환이 CPU 폭발(카메라가 앱 강제종료) ② 매니페스트 배선이 메타데이터를 끊음(감지 0). |
| ⏸ **결론** | 보류 — 무겁고 불안정 | 크롭먼저 NV12로 CPU는 잡았지만 배선 리스크가 커서 접음. 게다가 블러(저조도 모션)는 raw로도 못 고침. |
| 💡 **전환점** | 카메라가 이미 번호판을 잘라준다! | 메타데이터에 `<tt:ImageRef>/download/ch0/objectid_..jpg` 발견. good-shot 크롭이 파일로 있고, `/tmp` 붙이면 앱이 직접 읽힘. |
| ⭐ **해결** | 스냅샷·크롭·모션보정 통째로 폐기 | 카메라가 정렬해 자른 good-shot을 그냥 읽어 저장. 딜레이 0·정렬 완벽·초경량. **지금 구조의 뼈대.** |
| ❌ **문제** | 절반만 잡힘 / 거울상 / 중복 | 번호판 객체에 부모 차량 id가 0이라 스킵돼 저장 안 됨. 크롭은 좌우반전. 한 차가 good-shot 갱신으로 2번 저장. |
| 🔧 **해결** | id 조건 제거 · CSS 반전 · 객체id 중복제거 | 번호판 뜨면 무조건 저장. 반전은 브라우저 CSS로. 같은 객체id 갱신은 같은 슬롯 덮어써 **차 1대=1장.** |

> ⚠️ **끝내 소프트웨어로 못 넘은 벽** — 블러(모션블러 + 어두운 주차장)는 프레임이 찍히는 순간 이미 번져 있어 어떤 캡처방식으로도 못 지운다. 진짜 해결은 **카메라 셔터를 빠르게(하드웨어)** — 이번 범위 밖.

---

## 07 · 최종 코드 지도

**지금 앱에서 실제로 도는 핵심 함수·엔드포인트.**

| 이름 | 역할 | 단계 |
|---|---|---|
| `ProcessAEvent` | 이벤트 수신함 (메타데이터 / HTTP 분기) | 진입 |
| `HandleMetadataEvent` | XML 문자열 + 채널 꺼내 ProcessObjects 호출 | 2 |
| `ProcessObjects` | XML 스캔 → 객체·번호판·ImageRef 파싱, 움직임 판정 | 2 |
| `SaveCameraPlate` | 새 크롭이면 저장(객체id 중복제거 + pending) | 3 |
| `TrySavePlateFile` | 완성 JPEG이면 cap_N.jpg로 원본 저장 | 3 |
| `/platelist` · `/plate` | 갤러리용: 개수 JSON · 슬롯 이미지 | 4 |
| `/snapshot` · `index.html` | 라이브뷰(표시용, 캡처와 무관) · 웹 UI | 4 |

> ℹ️ **부하 정리** — 캡처 경로에 OpenCV 완전 제거. 3단계는 파일 읽기/쓰기뿐. 무거운 건 카메라가 다 함.

---

## 08 · 현재 상태 · 남은 것

**지금 어디까지 왔고, 다음은 무엇인가.**

### ✅ 되는 것

- 움직이는 차 번호판 캡처 — 카메라 good-shot 크롭
- 차 1대 = 1장 (객체id 중복제거)
- 거울상 교정 (표시), 순환 누적 갤러리
- 초경량 — OpenCV 제거, 순수 파일 I/O

### 🔜 한계 · 다음

- **블러** — 카메라 셔터 설정 필요(하드웨어)
- **가끔 놓침** — 봄/크롭/실패 카운터로 원인 추적 중
- **번호 텍스트** — 카메라가 앱에 안 줌 → 서버 OCR 등 별도
- **저장파일 거울상** — 실제 활용 시 서버에서 뒤집기

> ℹ️ **다음 검증** — 8대 영상 ×3 = 24대 돌려 **봄 / 크롭 / 총** 세 숫자로 남은 놓침이 카메라 탓(크롭 안 만듦)인지 우리 탓(못 읽음)인지 확정한다.
